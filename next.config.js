/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['ucarecdn.com','images.pexels.com','th.bing.com'],
  },
};

module.exports = nextConfig
